databases:
  - name: dpg-cj38k7enqql8v0f6pao0-a
    databaseName: mydb_zs8r_user
    user: mydb_zs8r_user

services:
  # A Docker web service
  - type: web
    name: fastapi-example
    runtime: python
    plan: free
    autoDeploy: false
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
